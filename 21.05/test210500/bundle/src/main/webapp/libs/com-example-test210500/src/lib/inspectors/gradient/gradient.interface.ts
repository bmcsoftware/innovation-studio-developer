import { Tooltip } from '@helix/platform/shared/api';

// This interface is used to type the inspector input parameters in the .design-model.ts file.
export interface IGradientOptions {
  required?: boolean;
  tooltip?: Tooltip;
}
